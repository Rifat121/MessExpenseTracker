name: Node.js CI

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: --experimental-vm-modules

    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    # Frontend steps
    - name: Cache frontend dependencies
      uses: actions/cache@v3
      id: frontend-cache
      with:
        path: |
          SharedExpenseManager/frontend/node_modules
          SharedExpenseManager/frontend/.cache
        key: ${{ runner.os }}-frontend-${{ hashFiles('SharedExpenseManager/frontend/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-frontend-

    - name: Install frontend dependencies
      working-directory: ./SharedExpenseManager/frontend
      run: |
        npm ci
        npm install -D tailwindcss@latest postcss@latest autoprefixer@latest

    - name: Build frontend
      working-directory: ./SharedExpenseManager/frontend
      run: npm run build
      env:
        NODE_ENV: production
        TAILWIND_MODE: build
